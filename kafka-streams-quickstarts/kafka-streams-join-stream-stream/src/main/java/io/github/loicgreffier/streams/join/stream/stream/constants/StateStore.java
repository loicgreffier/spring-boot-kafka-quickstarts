package io.github.loicgreffier.streams.join.stream.stream.constants;

import lombok.AccessLevel;
import lombok.NoArgsConstructor;

/**
 * This abstract class represents state store name constants.
 */
@NoArgsConstructor(access = AccessLevel.PRIVATE)
public abstract class StateStore {
    public static final String PERSON_JOIN_STREAM_STREAM_STATE_STORE =
        "PERSON_JOIN_STREAM_STREAM_STATE_STORE";
}
